<div class="container text-center">
  <% if @list.photo.attached? %>
    <%= cl_image_tag @list.photo.key, alt: "image", width: 600 %>
  <% end %>
  <h1 class="p-3"><%= @list.name %></h1>
  <%= link_to "Add a bookmark", new_list_bookmark_path(@list) %>
  <br>
  <br>
  <%= link_to "Back", lists_path  %>
</div>
